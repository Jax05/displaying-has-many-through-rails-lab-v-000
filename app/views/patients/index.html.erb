<h1>Patient Directory</h1>
<ul>
  <% @patients.each do |patient| %>
    <li>
      <%= link_to patient.name, patient %> - <%= pluralize(patient.appointments.count, 'appointment') %>
    </li>
  <% end %>
</ul>
